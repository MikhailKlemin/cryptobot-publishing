baseURL = "https://backtestdiscipline.com/"
title = "Backtest Discipline"
locale = "en-US"
enableRobotsTXT = true
disableKinds = ["taxonomy", "term"]

# RSS escapes XML explicitly; avoid a second pass through HTML auto-escaping.
[outputFormats.RSS]
  isPlainText = true

[params]
  description = "Published trading strategies, independently tested. Evidence-backed evaluations and practical research lessons by Mikhail Klemin."
  author = "Mikhail Klemin"

[menus]
  [[menus.main]]
    name = "Home"
    pageRef = "/"
    weight = 10
  [[menus.main]]
    name = "Research"
    pageRef = "/research"
    weight = 20
  [[menus.main]]
    name = "Methodology"
    pageRef = "/methodology"
    weight = 30
  [[menus.main]]
    name = "Notes"
    pageRef = "/notes"
    weight = 40
  [[menus.main]]
    name = "About"
    pageRef = "/about"
    weight = 50
